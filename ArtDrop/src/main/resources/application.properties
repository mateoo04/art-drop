spring.profiles.active=dev

spring.application.name=ArtDrop
spring.config.import=optional:file:.env[.properties]

logging.level.org.springframework.web=DEBUG
server.port=8089

spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5434}/${POSTGRES_DB:artdrop}
spring.datasource.username=${POSTGRES_USER:artdrop}
spring.datasource.password=${POSTGRES_PASSWORD:artdrop}

spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.ignore-migration-patterns=*:missing

jwt.token-validity-seconds=86400
jwt.base64-secret=${JWT_BASE64_SECRET}

app.seller.reapply-cooldown-days=14

feed.ranking.w-circle=1.5
feed.ranking.w-engage=1.0
feed.ranking.w-recency=2.0
feed.ranking.w-seen=3.0
feed.ranking.recency-half-life-hours=36
feed.ranking.engagement-half-life-hours=96
feed.ranking.seen-recency-half-life-hours=12
feed.ranking.candidate-window-days=30
feed.ranking.circle-window-days=90
feed.ranking.candidate-cap=300
feed.ranking.seen-window-days=7
feed.ranking.snapshot-ttl-minutes=5
feed.ranking.snapshot-max-entries=10000
feed.home.challenge.first-after-artworks=6
feed.home.challenge.every-n-artworks=18
feed.seen.dedupe-window-minutes=30
feed.seen.retention-days=14
feed.seen.cleanup-cron=0 30 3 * * *

spring.task.scheduling.pool.size=1

artdrop.featured-rotation-cron=0 * * * * ?

# Stripe
stripe.api-key=${STRIPE_SECRET_KEY:}
stripe.webhook-secret=${STRIPE_WEBHOOK_SECRET:}
stripe.success-url-base=${APP_BASE_URL:http://localhost:5173}/orders
stripe.cancel-url-base=${APP_BASE_URL:http://localhost:5173}/orders

# Commerce
commerce.platform-commission-rate=0.10
commerce.shipping-fee-eur=10.00
commerce.reservation-minutes=15
commerce.currency=EUR
commerce.reservation-cleanup-cron=0 */1 * * * ?
commerce.pending-order-grace-minutes=30
commerce.pending-order-cleanup-cron=0 */1 * * * ?
